<%= form_for(import, url: sales_path, html: { multipart: true }) do |f| %>
  
  <%= f.file_field :file, class: 'more-bottom' %>
  <br />
  <div class="actions">
    <%= f.submit class: 'button', class: 'btn' %> 
  </div>
<% end %>
